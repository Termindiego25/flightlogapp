<Window x:Class="PlaneDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wpfx="http://schemas.xceed.com/wpf/xaml/toolkit"
        xmlns:local="clr-namespace:Flight_LogApp"
        mc:Ignorable="d"
        Title="Plane" Height="275" Width="500" ResizeMode="NoResize" Icon="/Flight LogApp;component/images/logo.ico">
    <Grid VerticalAlignment="Center" Margin="20,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="Plate: " VerticalAlignment="Center" />
        <TextBox x:Name="plateTextBox" Grid.Column="1" Margin="10" TabIndex="1" />
        <TextBlock Text="Model: " Grid.Column="2" VerticalAlignment="Center" />
        <TextBox x:Name="modelTextBox" Grid.Column="3" Margin="10" TabIndex="2" />
        <TextBlock Text="Owner: " Grid.Row="1" VerticalAlignment="Center" />
        <ComboBox x:Name="ownerComboBox" Grid.Row="1" Grid.Column="1" VerticalAlignment="Center" Margin="10" TabIndex="3" IsEnabled="False">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock>
                        <TextBlock.Text>
                            <MultiBinding StringFormat="{}{0} {1}">
                                <Binding Path="name"/>
                                <Binding Path="lastname"/>
                            </MultiBinding>
                        </TextBlock.Text>
                    </TextBlock>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
        <TextBlock Text="Landing field: " Grid.Row="1" Grid.Column="2" VerticalAlignment="Center" />
        <ComboBox x:Name="landingFieldComboBox" Grid.Row="1" Grid.Column="3" DisplayMemberPath="name" VerticalAlignment="Center" Margin="10" TabIndex="4" IsEnabled="False" />
        <Button x:Name="sendPlaneButton" Grid.Row="2" Grid.Column="3" Margin="10" Content="Send" Click="sendPlaneButton_Click" TabIndex="5" />
    </Grid>
</Window>
